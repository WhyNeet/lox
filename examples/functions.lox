fun greeter(language) {
  var word = language == "en" ? "Hello" : "I only speak English,";

  fun greet(name) {
    print word + " " + name + "!";
  }

  return greet;
}

var english = greeter("en");
english("WhyNeet");

var other = greeter("es");
other("WhyNeet");
